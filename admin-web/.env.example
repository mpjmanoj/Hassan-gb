# Google Maps Platform — Maps JavaScript API key for the fleet live map.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Supabase (not wired yet — the dashboard runs on an in-memory store).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
